import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const completeOrder = () => {
  return (
    <div>
      <Title level={2}>completeOrder</Title>
      <p>这里是完成单子的页面</p>
    </div>
  );
};

export default completeOrder;